# --------------------------------------------------------------------------
# OpenClaw Governor — Vultr environment variables
# Copy to .env and fill in real values before deploying.
# --------------------------------------------------------------------------

# ── Governor Settings ──────────────────────────────────────────────────────
GOVERNOR_ENVIRONMENT=production
GOVERNOR_DATABASE_URL=postgresql://governor:changeme-db-password@postgres:5432/governor
GOVERNOR_JWT_SECRET=CHANGE-ME-use-python-c-import-secrets-print-secrets.token_urlsafe-48
GOVERNOR_ADMIN_USERNAME=admin
GOVERNOR_ADMIN_PASSWORD=CHANGE-ME-strong-password
GOVERNOR_ALLOW_CORS_ORIGINS=["https://openclaw-runtime-governor.vercel.app"]
GOVERNOR_LOG_LEVEL=info
GOVERNOR_LOG_FORMAT=json

# ── Postgres (used by the postgres container) ────────────────────────────
POSTGRES_PASSWORD=changeme-db-password

# ── Dashboard ─────────────────────────────────────────────────────────────
# The API URL the browser talks to. When served behind Caddy on the same
# domain, use an empty-origin relative path or the full public URL.
NEXT_PUBLIC_GOVERNOR_API=http://localhost:8000

# ── Optional ──────────────────────────────────────────────────────────────
# GOVERNOR_SURGE_GOVERNANCE_FEE_ENABLED=false
# GOVERNOR_SURGE_WALLET_ADDRESS=
# GOVERNOR_ENCRYPTION_KEY=
